# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:

- stage: Build
  displayName: 'Build source code'
  condition: succeeded()
  jobs:
    - job: Checkout_and_Build_Job
      displayName: 'Checkout & Build Project'
      steps:
        - script: echo Use .NET $(dotnetVersion)
          displayName: Use .NET $(dotnetVersion)
        - script: echo Gitleaks enabled $(owasp_dependency_check_enabled) gitleaks__scanlocation $(gitleaks__scanlocation) gitleaks__configfile $(gitleaks__configfile) gitleaks__baselinePath $(gitleaks__baselinePath) gitleaks__reportfolder $(gitleaks__reportfolder) 
          displayName: owasp_dependency_check_enabled enabled $(owasp_dependency_check_enabled)
        - checkout: self
          persistCredentials: true
          fetchDepth: 0
        - task: DotNetCoreCLI@2
          inputs:
            command: 'build'
